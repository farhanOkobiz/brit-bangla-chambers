import React from 'react';

const CaseDetails = ({_id}) => {
    console.log("Case ID:", _id);
    return (
        <div>
            Case Details
        </div>
    );
};

export default CaseDetails;